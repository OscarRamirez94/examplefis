@Configuration
public class RedisConfig {

    @Bean
    public RedisMessageListenerContainer redisContainer(RedisConnectionFactory connectionFactory) {
        // Este bean solo se declara si realmente necesitas pub/sub
        return new RedisMessageListenerContainer(); // pero puedes omitirlo
    }
}